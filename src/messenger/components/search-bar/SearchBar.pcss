.search-bar {
  display: flex;
  align-items: center;
  background: #f2f2f2;
  padding: 5px;
  border-radius: 10px;
  margin: 10px;
}

.search-input {
  flex-grow: 1;
  border: none;
  background: transparent;
  padding: 5px 10px;
  font-size: 14px;
}

.search-input:focus {
  outline: none;
}
